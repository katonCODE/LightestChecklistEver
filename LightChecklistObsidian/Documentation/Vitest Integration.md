# Vitest Integration

Integrated Vitest for automated testing in the project.

## Scripts Added
- `npm run test`: Executes the `vitest` test suite in the terminal.
- `npm run test:ui`: Launches the Vitest UI in the browser via `vitest --ui`.

**Related Files:**
- [[package.json]]
